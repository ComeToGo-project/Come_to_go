<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cometogo.ctg.admin.dao.UserBanDao">

    <insert id="insertUserBan" parameterType="com.cometogo.ctg.admin.dto.UserBanDto" useGeneratedKeys="true" keyProperty="banId">
        INSERT INTO ctg_user_ban (user_id, ban_start, ban_end, ban_status)
        VALUES (#{userId}, #{banStart}, #{banEnd}, #{banStatus});
    </insert>

    <update id="updateBanStatusByUser" parameterType="map">
        UPDATE ctg_user_ban
        SET ban_status = #{status}
        WHERE user_id = #{userId}
          AND ban_status = 'BAN';
    </update>
</mapper>